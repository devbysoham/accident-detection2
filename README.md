
  # Real-Time Accident Detection System

  This is a code bundle for Real-Time Accident Detection System. 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
